{% extends 'base.html.twig' %}

{% block title %}Modifier {{ group.name }}{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0" style="color: var(--text-color);">Modifier "{{ group.name }}"</h1>
        <a href="{{ path('app_group_index') }}" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-2"></i>Retour
        </a>
    </div>

    <div class="card shadow-sm" style="background-color: var(--card-bg);">
        <div class="card-body">
            {{ include('group/_form.html.twig', {'delete_form': true}) }}
        </div>
    </div>
</div>
{% endblock %}