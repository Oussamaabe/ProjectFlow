{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Two-Factor Authentication</h1>

    <div class="d-flex gap-3">
        <a href="{{ path('app_authenticator_pair') }}" class="btn btn-primary">Pair</a>
        <a href="{{ path('app_authenticator_verify') }}" class="btn btn-secondary">Verify</a>
    </div>
</div>
{% endblock %}
