{% extends 'base.html.twig' %}

{% block title %}Welcome!{% endblock %}

{% block body %}
<div class="container mt-5 text-center">
    <h1 class="mb-4">Welcome to Our Application</h1>

    <p>To continue, please sign in or sign up.</p>

    <div class="d-flex gap-3">
        <a href="{{ path('app_login') }}" class="btn btn-primary">Sign In</a>
        <a href="{{ path('app_register') }}" class="btn btn-secondary">Sign Up</a>
    </div>
</div>
{% endblock %}
