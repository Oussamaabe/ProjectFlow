{% extends 'base.html.twig' %}

{% block title %}CommandeAchat{% endblock %}

{% block body %}
    <h1>CommandeAchat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_achat.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ commande_achat.numero }}</td>
            </tr>
            <tr>
                <th>DateCommande</th>
                <td>{{ commande_achat.dateCommande ? commande_achat.dateCommande|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ commande_achat.statut }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ commande_achat.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_achat_index') }}">back to list</a>

    <a href="{{ path('app_commande_achat_edit', {'id': commande_achat.id}) }}">edit</a>

    {{ include('commande_achat/_delete_form.html.twig') }}
{% endblock %}
